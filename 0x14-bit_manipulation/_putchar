#include <unistd.h>
/**
 * _putchar - writes the character c to stdout
 * @c: The character to print the result of the varriable number to the code
 * Return: On success 1, On error -1 is returned
 * showing the char 
 */

int _putchar(char c)
{
	return (write(1, &c, 1));
}
